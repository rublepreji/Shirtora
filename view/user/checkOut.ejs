<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Checkout — Billing Details</title>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- SweetAlert2 for notifications and confirmation -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    <style>
      body { font-family: 'Inter', sans-serif; }
      
      /* Hide scrollbar for the coupon drawer */
      .no-scrollbar::-webkit-scrollbar { display: none; }
      .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

      .coupon-card {
        border: 1px dashed #e5e7eb;
        transition: all 0.2s ease;
      }
      .coupon-card:hover {
        border-color: #000;
        background-color: #f9fafb;
      }

      /* Transition for Coupon Drawer Animation */
      #couponDrawer {
        max-height: 0;
        opacity: 0;
        overflow: hidden;
        transition: max-height 0.4s ease-in-out, opacity 0.3s ease-in-out, margin-top 0.3s ease-in-out;
      }
      #couponDrawer.open {
        max-height: 500px; /* High enough to contain the list */
        opacity: 1;
        margin-top: 1rem;
      }
    </style>
  </head>
  
  <%- include("../../view/partials/user/header") %>

  <body class="antialiased text-gray-800 font-sans bg-gray-50/30">

    <div class="max-w-7xl mx-auto px-6 py-12">

      <!-- Breadcrumbs -->
      <div class="text-sm text-gray-500 mb-6">
        Account / Product / View Cart / <span class="text-gray-900 font-medium">CheckOut</span>
      </div>

      <h1 class="text-3xl md:text-4xl font-semibold mb-8">Billing Details</h1>

      <div class="grid grid-cols-1 md:grid-cols-12 gap-8">

        <!-- Left Column: Address and Payment -->
        <div class="md:col-span-7">
          <form id="checkoutForm" action="/placeorder" method="post">
            <section class="bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
              <!-- Delivery Address Header with Right-Aligned Add Button -->
              <div class="flex justify-between items-center mb-6">
                  <h2 class="text-lg font-medium">Delivery Address</h2>
                  <a href="/addnewaddress" class="text-[11px] font-bold bg-black text-white px-4 py-2 rounded shadow-sm hover:bg-gray-800 transition-all uppercase tracking-widest">
                      Add Address
                  </a>
              </div>

              <div class="space-y-4">
                <% if (addresses && addresses.length > 0) { %>
                  <% addresses.forEach((addr, index) => { %>
                    <label class="flex justify-between items-start block relative rounded-md border-2 <%= addr.isDefault ? 'border-black bg-gray-50/50' : 'border-gray-200' %> p-4 cursor-pointer hover:border-black transition-all">
                      <div class="pr-10"> 
                        <input 
                          type="radio" 
                          name="address" 
                          value="<%= index %>" 
                          <%= addr.isDefault ? 'checked' : '' %>
                          class="absolute right-4 top-4 h-4 w-4 text-black focus:ring-black border-gray-300" 
                        />

                        <p class="text-sm leading-relaxed text-gray-800">
                          <strong class="block text-base mb-1">
                            <%= addr.firstName %> <%= addr.lastName %>
                          </strong> 
                          <%= addr.addressLine %><br>
                          <%= addr.city %>, <%= addr.state %><br>
                          <%= addr.pincode %><br>

                          <span class="text-xs font-medium text-gray-500 mt-2 block">
                            Contact : +91 <%= addr.phone %>
                          </span>
                        </p>
                      </div>

                      <% if(addr.isDefault) { %>
                      <span class="h-fit flex-none self-start inline-flex items-center text-[10px] uppercase tracking-wider font-bold text-black bg-gray-200 px-2.5 py-1 rounded-full mt-0.5">
                          Default
                      </span>
                      <% } %>
                    </label>
                    
                    <% if (index === addresses.length - 1) { %>
                      <input type="hidden" name="selectedAddressIndex" id="selectedAddressIndex">
                      <input type="hidden" name="paymentMethod" id="paymentMethod">
                    <% } %>
                  <% }) %>
                <% } else { %>
                  <p class="text-red-500 text-sm italic">No address added yet</p>
                <% } %>
              </div>
            </section>

            <section class="mt-8 bg-white border border-gray-200 rounded-lg p-6 shadow-sm">
              <h3 class="text-lg font-medium mb-4">Payment Methods</h3>
              <p class="text-sm text-gray-500 mb-6">Choose your preferred payment option</p>

              <fieldset class="space-y-4">
                <label class="flex items-center gap-4 p-4 border border-gray-100 rounded-lg cursor-pointer hover:bg-gray-50 transition-colors">
                  <input type="radio" name="payment" value="UPI" class="h-4 w-4 text-black focus:ring-black border-gray-300" />
                  <div class="flex flex-col">
                    <span class="text-sm font-semibold">Razorpay</span>
                    <!-- <span class="text-xs text-gray-400">Pay using GPay, PhonePe, or BHIM</span> -->
                  </div>
                </label>

                <label class="flex items-center gap-4 p-4 border border-gray-100 rounded-lg cursor-pointer hover:bg-gray-50 transition-colors">
                  <input type="radio" name="payment" value="COD" class="h-4 w-4 text-black focus:ring-black border-gray-300" />
                  <div class="flex flex-col">
                    <span class="text-sm font-semibold text-gray-800">Cash on Delivery</span>
                    <!-- <span class="text-xs text-gray-400">Pay when your order is delivered</span> -->
                  </div>
                </label>

                <label class="flex items-center gap-4 p-4 border border-gray-100 rounded-lg cursor-pointer hover:bg-gray-50 transition-colors">
                  <input type="radio" name="payment" value="WALLET" class="h-4 w-4 text-black focus:ring-black border-gray-300" />
                  <div class="flex flex-col">
                    <span class="text-sm font-semibold text-gray-800">Wallet</span>
                    <!-- <span class="text-xs text-gray-400">Pay when your order is delivered</span> -->
                  </div>
                </label>
              </fieldset>
            </section>
          </form>
        </div>
        
        <!-- Right Column: Order Summary -->
        <aside class="md:col-span-5">
          <div class="bg-white border border-gray-200 rounded-xl p-6 sticky top-6 shadow-md"> 
            <h3 class="text-xl font-bold mb-6 border-b pb-4">Order Summary</h3>
            <ul class="divide-y divide-gray-100 mb-6">
              <% cartItems.forEach(item => { %>
                <li class="flex items-center py-4">
                  <img src="<%= item.productId.productImage[0] %>" alt="product image" class="w-16 h-20 object-cover rounded shadow-sm border border-gray-100 flex-shrink-0" /> 
                  <div class="flex-1 ml-4 overflow-hidden"> 
                    <div class="text-sm font-bold truncate text-gray-900 uppercase tracking-tight"><%= item.productId.name %></div> 
                    <div class="text-xs text-gray-500 mt-1">Quantity: <%= item.quantity %></div>
                  </div>
                  <div class="text-sm font-bold ml-4 text-gray-900">
                    ₹<%= (item.pricePerUnit * item.quantity).toLocaleString() %>
                  </div>
                </li>
              <% }) %>
            </ul>

            <div class="border-t border-gray-100 pt-6 space-y-3">
              <div class="flex justify-between text-sm text-gray-600">
                <span>Subtotal</span>
                <span class="font-semibold text-gray-900">₹<%= subtotal.toLocaleString() %></span>
              </div>

              <div class="flex justify-between text-sm text-gray-600 pb-2">
                <span>Shipping</span>
                <span class="font-bold text-green-600 uppercase text-xs">Free</span>
              </div>

              <!-- === COUPON SECTION === -->
              <div class="py-4 border-y border-gray-50 bg-gray-50/50 -mx-6 px-6">
                <label for="couponCode" class="block text-xs font-bold uppercase tracking-widest text-gray-400 mb-2">Apply Coupon</label>
                <div class="flex gap-2">
                    <div class="relative flex-1">
                        <input type="text" id="couponCode" placeholder="Enter code" class="w-full bg-white border border-gray-200 rounded-md pl-3 pr-8 py-2 text-sm font-medium focus:ring-1 focus:ring-black focus:border-black outline-none transition-all uppercase placeholder:normal-case">
                        <!-- Clear Button inside the input -->
                        <button type="button" id="clearCouponBtn" class="hidden absolute right-2.5 top-1/2 -translate-y-1/2 text-gray-400 hover:text-black transition-colors focus:outline-none">
                            ✕
                        </button>
                    </div>
                    <button type="button" id="applyCouponBtn" class="bg-black text-white px-4 py-2 rounded-md text-xs font-bold uppercase hover:bg-gray-800 transition-colors">
                        Apply
                    </button>
                </div>
                
                <!-- Toggle Button with Chevron -->
                <div class="mt-3">
                    <button type="button" onclick="toggleCouponDrawer()" class="text-[11px] font-bold text-indigo-600 hover:text-indigo-800 flex items-center gap-1 transition-all">
                        <svg id="couponChevron" class="w-3 h-3 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                        </svg>
                        SHOW AVAILABLE COUPONS
                    </button>
                    
                    <!-- Coupons List Drawer (Smooth Transition) -->
                    <div id="couponDrawer" class="space-y-3 no-scrollbar">
                        <div class="max-h-60 overflow-y-auto pt-2 space-y-3">
                            <% if (typeof availableCoupons !== 'undefined' && availableCoupons.length > 0) { %>
                                <% availableCoupons.forEach(coupon => { %>
                                    <div class="coupon-card p-3 rounded-lg bg-white shadow-sm flex justify-between items-center group">
                                        <div>
                                            <p class="text-xs font-black text-black tracking-widest uppercase"><%= coupon.couponCode %></p>
                                            <p class="text-[10px] text-gray-500 mt-0.5">Save <%= coupon.discountPercent %>% / upto <%=coupon.upto%> on your purchase </p>
                                        </div>
                                        <button type="button" onclick="copyAndFill('<%= coupon.couponCode %>')" class="text-[10px] font-bold bg-gray-100 group-hover:bg-black group-hover:text-white px-3 py-1.5 rounded transition-all">
                                            COPY
                                        </button>
                                    </div>
                                <% }) %>
                            <% } else { %>
                                <p class="text-[10px] text-gray-400 italic text-center py-2">No coupons available at the moment</p>
                            <% } %>
                        </div>
                    </div>
                </div>
                
                <div id="couponMessage" class="mt-2 text-[10px] hidden"></div>
              </div>

              <!-- Total Amount -->
              <div class="flex justify-between items-center text-xl font-bold pt-4 text-gray-900">
                <span>Total</span>
                <span id="grandTotalDisplay">₹<%= grandTotal.toLocaleString() %></span>
              </div>

              <button type="button" id="placeOrderTrigger" class="w-full bg-black text-white py-4 rounded-lg font-bold uppercase tracking-widest mt-6 hover:bg-gray-800 transform active:scale-95 transition-all shadow-lg">
                Place Order
              </button>
              
              <p class="text-[10px] text-gray-400 text-center mt-4">
                By placing your order, you agree to Shirtora's <a href="#" class="underline">Terms</a> and <a href="#" class="underline">Privacy Policy</a>.
              </p>
            </div>
          </div>
        </aside>
      </div>
    </div>
  </body>
  
  <%- include("../../view/partials/user/footer") %>

<script src="https://checkout.razorpay.com/v1/checkout.js"></script>
<script src="/js/user/checkOut.js"></script>
</html>