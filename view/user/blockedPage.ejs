<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Account Blocked</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gradient-to-br from-gray-100 via-white to-gray-200 flex items-center justify-center min-h-screen">

  <div class="bg-white shadow-2xl rounded-2xl p-10 w-full max-w-md text-center border border-gray-100">

    <!-- Warning Icon -->
    <div class="flex justify-center mb-6">
      <div class="bg-red-100 p-4 rounded-full">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-red-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m0 3.75h.007M12 4.5a7.5 7.5 0 100 15 7.5 7.5 0 000-15z" />
        </svg>
      </div>
    </div>

    <!-- Message -->
    <h1 class="text-2xl font-bold text-gray-800 mb-3">Account Blocked</h1>
    <p class="text-gray-600 mb-6">
      Your account has been temporarily blocked by the administrator.  
      Please contact support if you believe this is a mistake.
    </p>

    <!-- Info Message from backend -->
    <% if (typeof message !== 'undefined') { %>
      <div class="bg-red-50 text-red-600 text-sm px-4 py-2 rounded-lg mb-6 border border-red-200">
        <%= message %>
      </div>
    <% } %>

    <!-- Action Buttons -->
    <div class="flex justify-center gap-3">
      <a href="/logout" class="bg-gray-200 hover:bg-gray-300 text-gray-700 px-4 py-2 rounded-lg text-sm font-medium transition">
        Logout
      </a>
      <a href="/signin" class="bg-indigo-500 hover:bg-indigo-600 text-white px-4 py-2 rounded-lg text-sm font-medium transition">
        Go to Login
      </a>
    </div>

    <!-- Footer -->
    <p class="text-xs text-gray-400 mt-8">© <%= new Date().getFullYear() %> Admin Panel. All rights reserved.</p>
  </div>

</body>
</html>
