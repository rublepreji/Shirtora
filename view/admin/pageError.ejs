<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error | Admin Panel</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body class="bg-gray-50 min-h-screen flex">

    <!-- Sidebar -->
    <%- include("../../view/partials/admin/sideBar") %>

    <!-- Main Section -->
    <main class="ml-64 flex flex-col items-center justify-center w-full px-6 py-16 text-center">
      <div class="max-w-lg mx-auto">
        <!-- Error Illustration -->
        <div class="flex justify-center mb-8">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            class="w-32 h-32 text-red-500"
            stroke-width="1.5"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M12 9v4m0 4h.01M12 2a10 10 0 1010 10A10 10 0 0012 2z"
            />
          </svg>
        </div>

        <!-- Error Message -->
        <h1 class="text-5xl font-extrabold text-gray-800 mb-4">Oops!</h1>
        <p class="text-lg text-gray-600 mb-6">
          Something went wrong on this page or it doesn’t exist.
        </p>

        <p class="text-sm text-gray-400 mb-8">
          Error Code: <span class="font-medium text-gray-600">404</span>
        </p>

        <!-- Buttons -->
        <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
          <a
            href="/admin/dashboard"
            class="px-6 py-3 bg-indigo-600 text-white font-medium rounded-full hover:bg-indigo-700 transition-all"
          >
            ← Back to Dashboard
          </a>
          <a
            href="/"
            class="px-6 py-3 bg-gray-200 text-gray-800 font-medium rounded-full hover:bg-gray-300 transition-all"
          >
            Go to Homepage
          </a>
        </div>
      </div>
    </main>

    <!-- Responsive Sidebar Overlay (optional for mobile) -->
    <script>
      // You can add toggle logic for sidebar if you’re using one in mobile view
    </script>
  </body>
</html>
